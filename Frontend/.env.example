# Frontend — copy to .env and fill in your values

# Backend API URL (no trailing slash)
# Local:  http://localhost:5000
# AWS:    http://<ec2-ip>:5000
# Render: https://<service-name>.onrender.com
REACT_APP_API_URL=http://localhost:5000
